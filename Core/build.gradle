apply plugin: 'java-library'

dependencies {
    api "org.springframework.boot:spring-boot-starter-web:${boot}"
    api "org.apache.commons:commons-lang3:${lang3}"
    api "com.google.guava:guava:${guava}"
    api "io.swagger.core.v3:swagger-annotations:${swagger}"
}
